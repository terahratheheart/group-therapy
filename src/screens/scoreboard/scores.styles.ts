import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },

    buttons: {
        marginTop: 80
    },
    button: {
        marginBottom: 20
    },

    title: {
        color: "#ffffff",
        fontSize: 30,
        paddingBottom: 20,
        fontWeight: "bold"
    },

    subtitle: {
        color: "#ffffff"
    }
});

export default styles;
