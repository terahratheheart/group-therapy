import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        paddingTop: 120,
    },

    buttons: {
        marginTop: 80
    },
    button: {
        marginBottom: 20
    },

    title: {
        color: "#ffffff",
        fontSize: 45,
        paddingBottom: 20,
        fontWeight: "bold"
    },

    subtitle: {
        color: "#ffffff",
        fontSize: 25
    }
});

export default styles;
