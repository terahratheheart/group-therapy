import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        paddingTop: 120,
    },
    header:{
        color: "#ffffff",
        fontSize: 30,
    },
    body: {
        color: "#ffffff",
        fontSize: 20
    },
});

export default styles;
