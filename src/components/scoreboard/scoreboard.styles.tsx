import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: "row",
        marginTop: 5,
        marginBottom: 5
    },

    score: {
        color: "#ffffff",
        marginTop: 5
    },
    player: {
        color: "#ffffff",
        fontSize: 20,
    }
});

export default styles;
