import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    playerText: {
        color: "#ffffff",
        marginLeft: 10,
        marginTop: 13,
        fontSize: 16

    }
});
export default styles;
